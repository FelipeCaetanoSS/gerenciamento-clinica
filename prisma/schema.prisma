// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Get a free hosted Postgres database in seconds: `npx create-db`

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model Usuario{
  id    Int    @id @default(autoincrement())
  email String
  senha String
  senhaTemporaria Boolean @default(false)
  nome String
  idade Int
  sexo String
  rg  String  @unique
  cpf String @unique
  telefone String
  ativo Boolean @default(true)
  role  Role
  medico Medico?
  paciente Paciente?
  notificacao Notificacao[]
  alteradoPor Usuario? @relation("UsuarioAlteradoPor", fields: [alteradoPorId], references: [id])
  alteradoPorId Int?
  alteradoEm DateTime?
  usuariosAlterados Usuario[] @relation("UsuarioAlteradoPor")
  pacientesAlterados Paciente[] @relation("PacienteAlteradoPor")
  medicosAlterados Medico[] @relation("MedicoAlteradoPor")
  agendamentosAlterados Agendamento[] @relation("AgendamentoAlteradoPor")
  prontuariosAlterados Prontuario[] @relation("ProntuarioAlteradoPor")
  examesAlterados Exame[] @relation("ExameAlteradoPor")
}

enum Role {
  RECEPCIONISTA
  ADMIN
  MEDICO
  PACIENTE
}

model Medico{
  id    Int    @id @default(autoincrement())
  crm Int
  crmUf String?
  especialidade String?
  diasAtendimento String?
  horarioInicio String?
  horarioFim String?
  duracaoConsulta String?
  duracaoConsultaMinutos Int?
  maxConsultasDia Int?
  usuario Usuario? @relation(fields: [usuarioId], references: [id])
  usuarioId Int? @unique
  agendamento Agendamento[]
  receita Receita[]
  consulta Consulta[]
  exame Exame[]
  prontuario Prontuario[]
  alteradoPor Usuario? @relation("MedicoAlteradoPor", fields: [alteradoPorId], references: [id])
  alteradoPorId Int?
  alteradoEm DateTime?
}

model Paciente{
  id    Int    @id @default(autoincrement())
  localnasc String?
  DataNasc DateTime?
  estadoCivil String?
  tipoSanguineo String?
  peso Float?
  altura Float?
  alergia String?
  medicamento String?
  observacao String?
  deficiencia String?
  doencas String?
  usuario Usuario? @relation(fields: [usuarioId], references: [id])
  usuarioId Int? @unique
  agendamento Agendamento[]
  convenio Convenio[]
  endereco Endereco?
  receita Receita[]
  consulta Consulta[]
  exame Exame[]
  prontuario Prontuario[]
  alteradoPor Usuario? @relation("PacienteAlteradoPor", fields: [alteradoPorId], references: [id])
  alteradoPorId Int?
  alteradoEm DateTime?
}

model Agendamento{
  id    Int    @id @default(autoincrement())
  paciente Paciente @relation(fields: [pacienteId], references: [id])
  pacienteId Int
  medico Medico @relation(fields: [medicoId], references: [id])
  medicoId Int
  data DateTime
  status String @default("agendado")
  alteradoPor Usuario? @relation("AgendamentoAlteradoPor", fields: [alteradoPorId], references: [id])
  alteradoPorId Int?
  alteradoEm DateTime?
}

model Consulta{
  id    Int    @id @default(autoincrement())
  data DateTime
  status String
  motivo String
  paciente Paciente @relation(fields: [pacienteId], references: [id])
  pacienteId Int
  medico Medico @relation(fields: [medicoId], references: [id])
  medicoId Int
}

model Receita{
  id    Int    @id @default(autoincrement())
  medico Medico @relation(fields: [medicoId], references: [id])
  medicoId Int
  paciente Paciente @relation(fields: [pacienteId], references: [id])
  pacienteId Int
  medicamento String
  dosagem String
  dias  String 
  observacao String
  prontuario Prontuario[]
}

model Notificacao{
  id    Int    @id @default(autoincrement())
  titulo String
  mensagem String
  tipo String?
  categoria String?
  lida Boolean @default(false)
  data DateTime
  usuarioId Int?
  usuario Usuario? @relation(fields: [usuarioId], references: [id])
}

model Convenio{
  id    Int    @id @default(autoincrement())
  nome String
  numCarteirinha String
  validade DateTime
  paciente Paciente @relation(fields: [pacienteId], references: [id])
  pacienteId Int
}

model Exame{
  id    Int    @id @default(autoincrement())
  nome String?
  data DateTime @default(now())
  observacao String?
  imagem String?
  paciente Paciente @relation(fields: [pacienteId], references: [id])
  pacienteId Int
  medico Medico @relation(fields: [medicoId], references: [id])
  medicoId Int
  prontuario Prontuario? @relation(fields: [prontuarioId], references: [id])
  prontuarioId Int?
  anexoImagem String?
  alteradoPor Usuario? @relation("ExameAlteradoPor", fields: [alteradoPorId], references: [id])
  alteradoPorId Int?
  alteradoEm DateTime?
}

model Prontuario{
  id    Int    @id @default(autoincrement())
  data DateTime
  observacao String
  medicamento String?
  dosagem String?
  dias String?
  observacaoReceita String?
  prescricaoBaixadaPeloPaciente Boolean @default(false)
  prescricaoDownloadPacienteEm DateTime?
  medico Medico? @relation(fields: [medicoId], references: [id])
  medicoId Int?
  receita Receita? @relation(fields: [receitaId], references: [id])
  receitaId Int?
  paciente Paciente @relation(fields: [pacienteId], references: [id])
  pacienteId Int
  exames Exame[]
  alteradoPor Usuario? @relation("ProntuarioAlteradoPor", fields: [alteradoPorId], references: [id])
  alteradoPorId Int?
  alteradoEm DateTime?
}

model Endereco{
  id    Int    @id @default(autoincrement())
  rua String? 
  cidade String? 
  bairro String?
  numero Int?
  paciente Paciente? @relation(fields: [pacienteId], references: [id])
  pacienteId Int? @unique
}
